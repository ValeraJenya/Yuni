---
title: "Task 075 — lint-baseline вырос с 17 до 18"
weight: 75
---

## Статус

`idea` · P3 · зависит от Task 074

## Finding

Baseline предупреждений frontend-линтера держался на 17 начиная с Task 038
(PR #66, где он снизился с 18 до 17) — это число повторяется в отчётах
PR #72, #73, #74, #75 и #76. После PR #78 их стало 18.

Новое предупреждение — `react-hooks/set-state-in-effect` в
`apps/frontend/components/landing/motion/in-view.tsx:43`. Файл добавлен
коммитом `d9c74e4` в составе PR #78.

Отдельно стоит отметить: ни один из трёх коммитов PR #78 (`d9c74e4`,
`e21a9b7`, `3a196a3`) не содержит отчёта о прогоне линтера — в отличие от всех
пяти предыдущих frontend-PR, где число предупреждений указано явно. Рост
baseline поэтому и не был замечен при мерже.

## Почему нельзя чинить механически

Предупреждение стоит на `setVisible(true)` внутри ветки фолбэка:

```ts
// Older engines without IntersectionObserver get the content immediately
// rather than a block that never reveals.
if (typeof IntersectionObserver === "undefined") {
  setVisible(true)
  return
}
```

Эта ветка существует ровно затем, чтобы на движках без
`IntersectionObserver` контент показался сразу, а не остался навсегда
невидимым блоком. Убрать `setState` отсюда «чтобы линтер молчал» — значит
сломать путь деградации и получить пустой лендинг на старых браузерах вместо
предупреждения в консоли сборки.

Два допустимых способа:

1. **Ленивое начальное состояние** — вычислить доступность
   `IntersectionObserver` при инициализации `useState`, чтобы фолбэк не
   требовал `setState` в эффекте вообще.
2. **Точечный `eslint-disable`** на одной строке — с комментарием, объясняющим,
   зачем ветка нужна.

Это записано отдельной задачей и явным текстом именно потому, что следующий,
кто увидит 18 вместо 17, с высокой вероятностью «почистит» предупреждение
самым коротким способом и уберёт фолбэк вместе с ним.

## Scope

Вернуть baseline к 17 одним из двух способов выше, сохранив поведение на
движках без `IntersectionObserver`.

## Acceptance

- `pnpm --filter frontend lint` — 0 ошибок, 17 предупреждений;
- ветка фолбэка сохранена и по-прежнему показывает контент сразу при
  отсутствующем `IntersectionObserver`;
- если выбран `eslint-disable` — рядом есть комментарий с причиной.

## Зависимость

Task 074 правит тот же лендинг. Делать 075 раньше — значит дважды трогать одни
и те же файлы и разводить конфликты на ровном месте.

## Источник

Визуальная проверка PR #78 при синхронизации документации (Task 054),
2026-08-20.
