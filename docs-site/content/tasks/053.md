---
title: "Task 053 — Critical real-DB/HTTP coverage"
weight: 53
---

Статус: done
Приоритет: P2

Описание: Реальное покрытие заканчивается на Discovery; Like→Match→Chat→Block не имеет end-to-end journey tests.

Реализация: добавлен `apps/backend/test/match-block-chat.e2e-spec.ts` для real HTTP/DB цепочки Match → Conversation → Messages → Block/Unblock, а также concurrent сценариев block-vs-match, first game answers и stage-2 voice limit. Закрытие conversation при block уже покрыто Task 024 (тест проверяет это поведение, не переопределяет его); отдельно добавлена атомарная блокировка (`SELECT ... FOR UPDATE`) для подсчёта stage-2 voice total, чтобы обойти гонку из Task 044.

Один сценарий (`does not leave an active match when reciprocal like races with block`) намеренно помечен `it.skip` — он корректно фиксирует ожидаемое поведение для Task 042 (race condition блокировка vs match), но сама гонка в `matches.service.ts` ещё не исправлена (Task 042 всё ещё `idea`). Тест не удалён, а именно skip — как только Task 042 будет реализован, достаточно снять skip, а не писать тест заново.

Реально пройдено на CI (real Postgres): unit-тесты и HTTP/DB e2e для journey lifecycle (включая block → unblock permanent-closed), game-race concurrency и stage-2 voice limit concurrency — все зелёные, финальный прогон подтверждён 2026-07-31. Block-vs-match race — skipped до Task 042 (тест готов, ждёт фикса в `matches.service.ts`).

Мимоходом при верификации найден и исправлен read-path gap в Task 024 (чтение переписки не учитывало permanent-closed статус после unblock) — задокументирован в `024.md`.

Evidence: PR #49 (основная ветка, смержен) → PR #50 (unit-тесты после дедупликации) → PR #51 (handle length + rate-limit reset) → PR #52 (порядок сообщений + skip block-vs-match) → PR #53 (Task 024 read-path fix). Все смержены в `main`, CI зелёный.
