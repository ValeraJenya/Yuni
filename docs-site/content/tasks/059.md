---
title: "Task 059 — Backend lint — это tsc --noEmit"
weight: 59
---

Статус: done
Приоритет: P2

Описание: Реального линтера нет.

Реализация: для backend добавлен отдельный ESLint 9 flat config и script `lint: eslint .`; прежний `tsc --noEmit` сохранён отдельным script `typecheck` и запускается отдельным workflow-шагом.

Evidence: влито в `main` — PR #47 / merge `959c5d9` (тот же PR, что и Task 058).
Backend ESLint выполняется отдельным шагом от typecheck.
