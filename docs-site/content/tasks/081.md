---
title: "Task 081 — Non-standard autocomplete на /join, /profile и /onboarding"
weight: 81
---

## Статус

`done` · P3

## Повод

Инвентаризация 2026-08-20 зафиксировала консольный issue
`Non-standard autocomplete attribute value` на обоих экранах, но не
локализовала конкретное поле. Чтение кода не помогло: все семь значений
`autoComplete` в этих формах (`email`, `current-password` —
`sign-in-form.tsx:148,161`; `given-name`, `username`, `email`,
`new-password` — `sign-up-form.tsx:265,275,285,298`; `bday` —
`birthdate-field.tsx:94`) по отдельности валидны по спецификации.

## Локализация — живой DevTools, не статика

Через `chrome-devtools-mcp` (`list_console_messages` с типом `issue`,
`get_console_message` для деталей) на реально запущенном dev- и
production-сервере.

### `/join` — воспроизводится стабильно

Виновник: `#birthdate` в `features/auth/components/birthdate-field.tsx:94`,
`autoComplete="bday"`.

Изолировано заменой значения:

| `autoComplete` на `#birthdate` | Issue на `/join` |
| --- | --- |
| `"bday"` (исходное) | есть (count: 1) |
| `"off"` | нет (count: 0) |
| `"bday"` (возврат) | снова есть |

Воспроизведено одинаково в dev и в production-сборке (`next build` +
`next start`).

### `/signin` — не воспроизводится ни разу

Проверено:
- свежая загрузка, dev-сервер — issue нет;
- свежая загрузка, production-сборка — issue нет;
- новая, никогда не открывавшая `/join`, вкладка — issue нет;
- клик «Показать пароль» (реальное переключение `type` поля пароля с
  `"password"` на `"text"` при неизменном `autoComplete="current-password"`)
  — issue не появляется.

### Второе срабатывание — не второй баг, а артефакт SPA-навигации

Проверка перехода `/signin` → (клик «Создать профиль», клиентский роутинг
Next.js, без полной перезагрузки) → `/join` → (клик «Войти», снова
клиентский переход) → `/signin`: issue **появляется** на `/signin`.

Но его данные:

```
data={"violatingNodeAttribute":"autocomplete","uid":"16_22"}
```

`uid=16_22` — это узел «ДАТА РОЖДЕНИЯ» со **страницы `/join`**, снятый в
предыдущем `take_snapshot`, не элемент `/signin`. После обычного
`reload()` issue немедленно пропадает.

**Вывод**: панель DevTools Issues не сбрасывается при клиентской
(pushState) навигации Next.js — только при полной перезагрузке страницы.
Инвентаризация 2026-08-20, судя по всему, переходила между `/join` и
`/signin` по ссылкам внутри приложения, и одна и та же находка (birthdate
на `/join`) задвоилась в отчёте под видом второго независимого случая.
**Реальный дефект один.** `auth-shell.tsx` и `auth-field.tsx` (общие
компоненты, вторая проверенная гипотеза) в этом не участвуют — оба чистые,
проверены все 7 полей на обеих формах по отдельности.

## Проверка гипотезы «сочетание с `type`/`inputMode`»

Отдельно проверено и отвергнуто:

| Конфигурация `#birthdate` | Issue |
| --- | --- |
| `type="text"`, `inputMode="numeric"`, `autoComplete="bday"` (исходное) | есть |
| `type="text"`, без `inputMode`, `autoComplete="bday"` | есть |
| `type="date"`, без `inputMode`, `autoComplete="bday"` | **есть** |

Issue не зависит ни от `inputMode`, ни от типа поля — Chrome не признаёт
значение `"bday"` валидным для этого элемента ни в одной из проверенных
конфигураций, хотя по спецификации WHATWG `bday` — легитимный токен.
Список валидных значений в Chrome расходится со спекой (тот же класс
расхождения, что у документированного отдельно случая с `nickname`,
который Chrome тоже не признаёт).

## Правка

`autoComplete="off"` вместо `"bday"` на `#birthdate`
(`birthdate-field.tsx:94`), с комментарием на месте: почему `off`, а не
`bday`, и что настоящее решение (нативный `type="date"` либо три поля
`bday-day`/`bday-month`/`bday-year`) — отдельная задача, не эта.

**Почему `off` не теряет функциональность.** Помимо самого issue,
`handleChange` в том же файле безусловно прогоняет значение через
`digitsOnly(e.target.value)` — берёт все цифры из строки, не разбирая их
исходный порядок. Если бы Chrome всё же попытался подставить дату (в
формате `YYYY-MM-DD`, как это делает автозаполнение для `type="date"`),
маска перемешала бы цифры года/месяца/дня в неверном порядке и записала
бы не ту дату. То есть даже без issue поле в текущем виде не могло бы
корректно принять автоподстановку.

**Что не удалось проверить напрямую.** Живое сохранённое значение `bday` в
автозаполнении Chrome живёт в разделе «Автозаполнение и пароли → Удостоверения
личности», который в этом окружении неактивен без входа в Google-аккаунт
(`chrome://settings/identityDocs` — переключатель и кнопка «Добавить»
задизейблены). Подставить тестовые данные и увидеть подсказку браузера
вручную не получилось — вывод об отсутствии потери функциональности
основан на (1) самом issue (Chrome не считает элемент валидной целью для
автозаполнения) и (2) разборе `handleChange` выше, а не на прямом
наблюдении подсказки автозаполнения.

Автозаполнение остальных полей (email, current-password, given-name,
username, new-password) не тронуто и не пострадало — они не менялись.

## Проверка после правки birthdate

- `/join`: issue пропал (было count 1, стало 0), проверено в dev и prod;
- `/signin`: чист как и раньше;
- сценарий SPA-перехода `/join` → `/signin` без reload: issue тоже не
  появляется — источник (birthdate) исправлен, переносить стало нечего.

## Второй виновник — `nickname` на `/profile` и `/onboarding`

После находки про SPA-навигацию встал вопрос: в форме анкеты
(`features/profile/form-state.ts`, `PROFILE_FORM_FIELDS`) есть ещё три
значения `autoComplete` — `nickname` (поле «Имя»), `address-level2`
(город), `country-name` (страна). Эта анкета рендерится и на `/profile`
(режим редактирования), и на `/onboarding` — оба экрана не упомянуты в
инвентаризации 2026-08-20 как затронутые.

Проверено методом из раздела выше — **полной перезагрузкой каждого
экрана отдельно**, не переходом по ссылке:

| Экран | Как открыт | Issue |
| --- | --- | --- |
| `/profile` | `navigate` на URL (`ignoreCache`), затем клик «Редактировать» — поля анкеты не в DOM до этого клика | есть (count: 1) |
| `/onboarding` | прямой `navigate` на URL (`ignoreCache`); поля видны сразу, редиректа со `/signin` не было — заходил напрямую | есть (count: 1) |

В обоих случаях `count: 1`, не 3 — `address-level2` и `country-name`
чистые. Изолировано тем же способом, что и `bday`: временная замена
`autoComplete: "nickname"` → `"off"` в `form-state.ts` убирает issue на
`/profile` (проверено полной перезагрузкой + повторным входом в
редактирование), возврат `"nickname"` — возвращает.

Это не артефакт SPA-навигации из раздела выше: на `/profile` между
загрузкой страницы и появлением issue нет никакого перехода — только
клик, открывающий панель редактирования в том же документе; на
`/onboarding` поля видны сразу после прямой полной загрузки URL.

**Правка**: `autoComplete: "off"` вместо `"nickname"` в
`PROFILE_FORM_FIELDS` (`form-state.ts`), с комментарием на месте.
Функциональность не теряется по той же логике, что и с `bday`: раз
Chrome не считает элемент валидной целью для автозаполнения (то самое
issue), реального автозаполнения по этому токену не было и до правки.

## Проверка после обеих правок

- `/join`: issue пропал (было count 1, стало 0), проверено в dev и prod;
- `/signin`: чист как и раньше, включая сценарий SPA-перехода с `/join`;
- `/profile` (режим редактирования): issue пропал после замены
  `nickname` → `off`, проверено полной перезагрузкой;
- `/onboarding`: issue пропал, проверено прямой полной загрузкой URL;
- `pnpm --filter frontend lint`: 17/17 (без изменений);
- `tsc --noEmit` — чисто;
- `next build` — 17 роутов, без ошибок;
- `jest` — 60/60 (включая `features/profile/form-state.test.ts`).

## Методический вывод

Записан в `CLAUDE.md` (раздел «Визуальная проверка» → «Панель Issues и
клиентская навигация»): проверка через Issues делается полной
перезагрузкой каждого экрана, не переходом по ссылке — иначе находка
может быть приписана не тому месту, как чуть не произошло с `/signin`
в этой же задаче. Отмечено, что `docs/audits/frontend-inventory-2026-08-20.md`
собран без этого правила и мог приписать issue не тем экранам — сам
отчёт не редактировался.
