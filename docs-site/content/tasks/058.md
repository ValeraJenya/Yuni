---
title: "Task 058 — CI не может запустить e2e"
weight: 58
---

Статус: review
Приоритет: P2

Описание: В workflow нет services блока с Postgres.

Реализация: в `.github/workflows/quality-gates.yml` добавлен service `postgres:16` с базой `yuni_ci` и credentials `postgres/postgres`, совпадающими с `DATABASE_URL`. Workflow выполняет `prisma:migrate:deploy` и отдельный `pnpm --filter backend test:e2e`.

Evidence: изменения подготовлены в ветке `codex/task-058-ci-postgres-and-lint`; фактические проверки фиксируются в отчёте задачи.
