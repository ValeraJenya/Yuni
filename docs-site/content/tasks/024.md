---
title: "Task 024 — Atomic block and match lifecycle"
weight: 24
---

## ID

024

## Название

Atomic block and match lifecycle

## Статус

done

## Приоритет

P1

## Зависимости

- Task 000 (Project documentation foundation) — done

## Описание

`endActiveMatchesBetween()` теперь атомарно закрывает и `Match`, и связанный `Conversation` в одной транзакции с созданием `Block`.

## Что реализовано

- `blockUser()` в транзакции создаёт `Block` и переводит `Match.status` в `blocked` (PR #31).
- `endActiveMatchesBetween()` дополнительно переводит `Conversation.status` из `active` в `closed` для матчей между теми же двумя пользователями, в той же транзакции.
- `findWritableConversationOrThrow()` уже отклоняет отправку сообщений при `status !== active` — с этим изменением проверка начинает реально срабатывать при блокировке.
- Как и `Match` (который не возвращается в `active` при unblock), `Conversation` остаётся `closed` навсегда — `unblockUser()` намеренно не переоткрывает переписку.
- Тесты: `moderation.service.spec.ts` — обновлены happy-path проверки на `blockUser`/duplicate-block пути + новый тест на откат при сбое `conversation.updateMany`.

## Что остаётся

Ничего — PR #38 смержен, юнит-тесты зелёные на CI. Дополнительно найден и закрыт read-path gap (см. Evidence) при подготовке real-Postgres e2e для Task 053.

## Evidence

- PR #31 / commit `19655b8` — транзакционный Block + Match lifecycle (частичная реализация)
- PR #38 / merge `f027b0f` (commit `09253ef`) — Conversation закрывается атомарно вместе с Match; смержено в `main`, юнит-тесты зелёные на CI
- `apps/backend/src/modules/moderation/moderation.service.ts` — `endActiveMatchesBetween()`
- `apps/backend/src/modules/moderation/moderation.service.spec.ts`
- `apps/backend/src/modules/chat/chat.service.ts` — `findWritableConversationOrThrow()` (существующая проверка, из-за которой фикс становится действующим) и `findConversationForRead()` (read-path fix ниже)
- **Read-path gap, найден real-Postgres e2e тестом из Task 053** (`match-block-chat.e2e-spec.ts`): `findWritableConversationOrThrow()` (запись) проверяет `Conversation.status`, но `findConversationForRead()` (чтение — `getMessages`, `getConversationStage` и т.д.) проверял только текущее наличие live `Block`, а не статус. После `unblockUser()` (который удаляет `Block`, но статус не трогает) переписка становилась снова читаемой, хотя должна оставаться закрытой навсегда — прямое нарушение задокументированного здесь же намерения. Исправлено добавлением `status: ConversationStatus.active` в `findConversationForRead()`, по аналогии с уже существующей проверкой в `getConversations()`.

## История статуса

- 2026-06-26: добавлена в roadmap.
- 2026-07-21: статус изменён на `in_progress`; PR #31 зафиксирован как частичная реализация, оставшийся Conversation/chat gap описан явно.
- 2026-07-29: статус изменён на `review`; Conversation.status закрывается атомарно вместе с Match, тесты обновлены — ожидает прогона тестов и мержа.
- 2026-07-31: статус изменён на `done`; PR #38 смержен, юнит-тесты зелёные. Read-path gap (чтение переписки после unblock) найден и закрыт отдельным фиксом при верификации Task 053.
