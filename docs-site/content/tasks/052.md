---
title: "Task 052 — Переключатель языка неполон"
weight: 52
---

Статус: done
Приоритет: P2

Описание: Не персистится, не обновляет html lang, разные instance провайдера.

Результат: Закрыта в PR #68 / merge `55163e4`. Один `LangProvider` в корневом
layout вместо нескольких независимых instance; выбор хранится в `localStorage`
через `useSyncExternalStore`, `document.documentElement.lang` обновляется,
язык синхронизируется между вкладками.

Хвост: строка «Loading» в app-shell осталась нелокализованной и была
исправлена попутно в PR #74 (Task 030) — в PR #68 был поправлен только auth
layout.
